# Secrets — never commit
.env
.env.local
.env.*.local
*.key
*.pem

# Server signing keys (secret)
server/.keys/

# Node
node_modules/
dist/
.next/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
.vscode/
.idea/

# Vendored reference (codeplain's own example repo, not our work)
plainlang-examples/

# Codeplain rendered output (build artifacts — regenerated from .plain, never committed)
/web/
/plain_modules/
/build/
/conformance_tests/
/dist_conformance_tests/
/.tmp/
codeplain.log
.claude/
skills-lock.json
.gstack/

# Runtime-generated verify receipts (from "Prove it"); keep only the baked demo records
server/glassbox/static/receipts/*.json
!server/glassbox/static/receipts/latest.json
!server/glassbox/static/receipts/1fa7f4150fb53178.json
